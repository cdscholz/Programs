package Project3Package.HelpfulNotes.ClassesAndObjectsPractice;

public class PackagesAccessModifiers {
    public static void main(String[] args) {

        //Packages --> Organizing and categorizing classes and related resources into a hierarchical structure
        //1. Namespace Management
        //2. Code Organization
        //3. Access Control
        //4.

        //Access Modifiers --> Apply to Car class
        //Controls the visibility and accessibility of classes, methods, and variables
        //Access Modifiers:
        //Public --> The class, method or a variable can be accessed from any other class or package (no access restrictions)
        //Private --> The class is only accessible within the same class, can't access from any other class or package
        //Protected --> The accessible within the same package and by subclasses, even if they are in different packages. Subclasses from different packages can access protected members only through inheritance
        //Package-Private(Default) --> If no access modifier is specified, the cass member has package-private access, It is accessible within the same package but not from outside the package








    }
}
